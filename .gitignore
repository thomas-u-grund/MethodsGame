# macOS
.DS_Store

# Everything superseded but not thrown away: the abandoned Godot prototype, the orphaned
# single-room HTML prototypes that web/the-secret-of-the-codebook.html replaced, and stale
# local backups. None of it was ever committed, so it lives here rather than being deleted.
_attic/

# Unrelated personal files
Wes Anderson/

# Full-size PNG originals of the WebP assets in web/ (kept locally, too big for the repo)
web-png-originals/

# Local text-to-speech environment (Chatterbox) and its model cache
.venv-tts/
tools/tts/out/

# Scratch test harness output
scratchpad/

# Working screenshots for review; regenerated on demand, not part of the game
screenshots/

# The author's Christmas-lecture source material (2.2 GB deck + videos); the game uses
# extracts of it, archived under art/ and audio/rap/.
rap battle/
__pycache__/
